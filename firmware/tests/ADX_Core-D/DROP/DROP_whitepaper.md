<!--
Copyright (c) 2026 ADX Project Contributors
SPDX-License-Identifier: CC-BY-4.0
-->

# 産業用フィールドネットワーク DROP-Bus 仕様書・ホワイトペーパー

**DROP-Bus** (**D**eterministic **R**elay with **O**ptional-mediator & **P**assive-failsafe Bus)

---

## 1. エグゼクティブサマリー＆設計思想 (Executive Summary & Core Philosophy)

### 1.1 背景と課題（既存フィールドバスの限界）
現代の産業用・組み込み用分散制御システム（多軸ロボット、搬送ライン、センサ・アクチュエータネットワーク等）において、リアルタイム通信プロトコルの選定は常に厳しいトレードオフに直面してきました。

* **CAN / CAN-FD:** 優先度調停（CSMA/CD）と自動再送機構を内包するため、高負荷時やノイズ発生時に通信ジッターが肥大化し、最悪実行時間（WCET: Worst-Case Execution Time）の見積もりが極めて困難。
* **LIN:** 低コストかつオートボーレート同期を備えるが、マスター・スレーブ依存が強く（SPOFリスク）、データ長が最大8バイト、通信速度が最大19.2 kbpsと制御用途には帯域不足。
* **Modbus-RTU / RS-485 ポーリング:** 呼出・応答（Poll/Response）による通信オーバーヘッドが大きく、バス利用効率が低い。
* **一般的なトークンリング / 産業用イーサネット:** 高速かつ決定論的であるが、各ノードに高精度な水晶発振器や専用コントローラIC（PHY/MAC）が必須となり、ノード単価と基板面積を押し上げる。

DROP-Bus は、これらの課題を根本から打破するために**「極限の低コスト（単一SKU・完全水晶レス）」**、**「厳格な決定論性（ジッター拘束）」**、そして**「受動的フェイルセーフ（沈黙＝安全）」** を三位一体で実現する新しい自律分散型フィールドバス規格です。

---

### 1.2 コア設計思想

```text
=============================================================================
                      DROP-Bus の 3大コア哲学
=============================================================================
 1. 沈黙は安全 (Silence is Safety)
    - 通信途絶・パケット破損時は自律再送やスキップを一切行わない
    - バトンが落ちた瞬間に全ノードが同期して即時安全停止（Safe Torque Off）

 2. メカニズムとポリシーの完全分離 (Separation of Mechanism & Policy)
    - プロトコル・コアは「バトンリレー」と「沈黙停止」のみを極小実装
    - 再送回数・介入・ジッターバジェットは上位ポリシー（Mediator / Plugin）に委ねる

 3. 単一SKU・完全水晶レス (Crystal-less Single SKU)
    - 末端ノードは内蔵RC発振器の低コストMCU（ATtiny1616等）で動作
    - フレーム先頭の LINAUTO (Break + 0x55) 物理同期により毎周クロックを自動校正
=============================================================================
```

1. **沈黙は安全（Silence is Safety）**
   * プロトコル層がブラックボックスで再送やノードスキップを試みる「勝手な継続動作」を全廃。
   * バトンが脱落（DROP）した場合、バスは無音となり、全ノードの内蔵ハードウェアタイマーが一斉に満了してアクチュエータ出力を安全遮断（STO: Safe Torque Off）します。
2. **メカニズムとポリシーの完全分離（Decoupled Architecture）**
   * プロトコル・コア（Core）はバトンの受け渡しと受動的停止のみに徹します。
   * 「再送を行うか」「何ミリ秒待つか」「どのように復旧するか」といった時間軸の制御は、外付けまたは内包された **Mediator（調停者）** および **再送プラグイン** の裁量下に置かれます。
3. **単一SKU・完全水晶レス（Crystal-less Single SKU）**
   * すべての末端ノードは同一のハードウェア構成・ファームウェア（単一SKU）で動作可能です。
   * 高価な外付け水晶発振器を不要とし、フレーム単位の物理同期によって温度・電圧変動による内蔵RCドリフトを完全に吸収します。

---

## 2. 用語定義と論理ロール (Terminology & Logical Roles)

### 2.1 コア用語

| 用語 | 英語表記 | 定義と役割 |
| :--- | :--- | :--- |
| **ノード** | Node | ネットワークの構成員。単一SKUで動作し、自身宛てのバトンを受信したら次ノードへデータを送信してリレーを繋ぐ。異常時は即座に沈黙する。 |
| **バトン** | Baton | 各フレームに含まれる「次に発話すべきノードの指定権（`TARGET_ID`）」。バトンを保持する単一ノードのみが送信権を持つ。 |
| **ドロップ** | Drop | 通信エラー、未応答、ノード脱落、または調停シグナルによってバトンが失われ、バスが完全な無音状態に陥る現象。 |
| **パッシブ・フェイルセーフ** | Passive Fail-Safe | バトンドロップ発生時、全ノードが再送を行わず、ハードウェアタイマーの満了によって同期して安全停止（STO）へ移行する保護動作。 |
| **メディエーター** | Mediator | システムに任意で配置可能な**調停者（論理ロール）**。平常時は無音でバスを傍受し、バトンドロップ時や保守時のみ介入（衝突または再点火）を行う。 |

---

### 2.2 概念（論理ロール）としての Mediator

DROP-Bus における Mediator は「特定の専用ハードウェア」を指す言葉ではありません。**「落ちたバトンを拾い直す、またはバスの秩序を書き換える権能を持つ論理的な役割（Role）」** です。

システム要件に応じて、以下の4形態を自由に選択・配置できます。

```text
【形態1: 完全マスターレス構成 (No Mediator)】
[Node 1] <───(RS-485 2-Wire)───> [Node 2] <───(RS-485 2-Wire)───> [Node 3]
※ 最安・最軽量構成。ドロップ時は全系が潔く即時安全停止し、外部リセットまで待機。

【形態2: ノード内包型 (Co-located Mediator)】
[Node 1 (Runner + Mediator)] <───(RS-485)───> [Node 2] <───(RS-485)───> [Node 3]
※ PLC直結ノードや処理能力に余裕のあるNode 1に調停ロジックを同居させる。

【形態3: 外付けアタッチメント型 (Dedicated Mediator)】
[Node 1] ─── [Node 2] ─── [Node 3] ─── (RS-485) ─── [ Dedicated Mediator ]
※ 末端は純粋な単一SKU。上位通信ゲートウェイや診断ロガーとして別基板を追加。

【形態4: 保守ツール型 (Maintenance Dongle)】
[Node 1] ─── [Node 2] ─── [Node 3] ─── [ (Service Port) <--- (Handheld Tool) ]
※ 通常時はマスターレスで稼働し、保守員が活線挿抜や構成変更を行う際のみ接続。
```

---

## 3. 物理層およびクロックレス同期設計 (Physical Layer & Synchronization)

### 3.1 物理層構成
* **伝送路:** RS-485 半二重 2線式差動伝送（A / B / GND）
* **トポロジー:** デイジーチェーン / マルチドロップ（スタブ長は極小化）
* **終端抵抗:** バス両端に 100〜120 $\Omega$（ADX Core-D 標準ジャンパ対応）
* **フェイルセーフバイアス:** バス解放時の不定電位・ノイズ誤検知を防ぐため、プルアップ/プルダウンによるフェイルセーフバイアスを推奨

```text
[Node 1]               [Node 2]               [Node N]
+--------+             +--------+             +--------+
| ATtiny |             | ATtiny |             | ATtiny |
|  1616  |             |  1616  |             |  1616  |
+---+----+             +---+----+             +---+----+
    | UART                 | UART                 | UART
+---+----+             +---+----+             +---+----+
| SP485  |             | SP485  |             | SP485  |
+---+----+             +---+----+             +---+----+
    | A/B                  | A/B                  | A/B
====+======================+======================+===== RS-485 Bus (2-Wire)
```

---

### 3.2 完全水晶レス設計（Crystal-less Operation）
DROP-Bus では、全末端ノードがマイコン内蔵の RC 発振器（ATtiny1616 内部 16MHz/20MHz オシレータ等）で駆動します。

* **短期周波数安定性:** 内蔵RC発振器は、1フレームの伝送時間（数百 $\mu\text{s}$ 〜 数 $\text{ms}$）内であれば、温度や電源電圧の急峻な変化を受けず、極めて高い短期周波数安定性を誇ります。
* **累積誤差のゼロリセット:** フレーム先頭で受信する同期シグナルごとにボーレート設定レジスタ（`BAUD`）を直接ハードウェア補正するため、長期的なクロックドリフトは完全に相殺されます。

---

### 3.3 LINAUTO 同期メカニズム

UART物理層には、車載LIN規格に準拠した **LINAUTO（Hardware Auto-Baudrate）** 方式を採用します。

```text
[DROP-Bus 物理伝送波形]
       |<--- Break Field --->| Delimiter |<----- Sync Byte ----->|
TX: ───┐                     ┌───────────┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─────┴─[ LEN ] [ TARGET_ID ] ...
       └─────────────────────┘           └─┘ └─┘ └─┘ └─┘ └─┘
       |<--- 13~14 bit 0 --->| (>=1bit 1)|<--- 0x55 (測定) ----->|
```

1. **Break Field 送出:**
   送信ノードは、フレーム先頭で 13〜14 ビット時間相当の連続 Low（Dominant）を送出します。
2. **Break 検出:**
   受信側マイコン（USART LINAUTO モード）は、11 ビット以上の連続 Low を検出すると「新フレーム開始」と判定し、ハードウェア計測タイマーを待機状態にします。
3. **Delimiter:**
   1 ビット以上の High（Recessive）期間を置き、立ち下がりエッジを明確化します。
4. **Sync Byte (`0x55`) による自動校正:**
   続く `0x55`（2進表記: `01010101`）のエッジ間隔をハードウェアタイマーが自動測定し、受信USARTのボーレートレジスタを送信側の実周波数に同期校正します。
5. **ペイロード受信:**
   後続するデータバイト列は、校正されたボーレートによってサンプリング誤差ゼロで確実に受信されます。

---

## 4. データフレーム仕様 (Data Frame Specification)

### 4.1 フレームフォーマット

最大64バイトの可変長ペイロードに対応し、高い誤り検出能力を持つ **CRC-16-CCITT** を採用します。

```text
|<------------- 物理同期ヘッダ ------------->|<------------------------- 論理データフレーム ------------------------->|
+---------+------+-------+-----+-----------+-----------+-----------------------------------+---------+
|  BREAK  | DEL  | SYNC  | LEN | TARGET_ID | SENDER_ID |       PAYLOAD (0 .. N Byte)       | CRC-16  |
+---------+------+-------+-----+-----------+-----------+-----------------------------------+---------+
  >=13bit   1bit    1B     1B       1B          1B              可変長 (最大64 Byte)              2B
```

---

### 4.2 フィールド定義

| フィールド | サイズ | 設定値 / 範囲 | 説明 |
| :--- | :---: | :---: | :--- |
| **BREAK** | $\ge 13$ bit | Dominant (0) | ハードウェア LINAUTO トリガー用の連続 Low 信号 |
| **DEL** | $\ge 1$ bit | Recessive (1) | ブレーク・デリミタ（立ち上がり区間） |
| **SYNC** | 1 Byte | `0x55` | オートボーレート計測用の交番ビットパターン |
| **LEN** | 1 Byte | `0x00` 〜 `0x40` | 後続する PAYLOAD のバイト数 $N$（最大64バイト） |
| **TARGET_ID** | 1 Byte | `0x00` 〜 `0xFE` | バトンを受け取る宛先ノードID（`NEXT_ID`） |
| **SENDER_ID** | 1 Byte | `0x00` 〜 `0xFE` | 現在送信している自機ノードID（`MY_ID`） |
| **PAYLOAD** | $N$ Byte | 任意データ | 制御指令、トルク値、センサ値、ステータスフラグ等 |
| **CRC-16** | 2 Byte | 計算値 | `LEN` 〜 `PAYLOAD` 末尾までを対象とする誤り検出コード |

* ※ `0xFF` は Mediator による全ノード同報・調停用のブロードキャストIDとして予約。

---

### 4.3 マイコン（ATtiny1616）における実装防御機構

極小リソースのMCU上でバッファ破壊やノイズによる暴走を防ぐため、受信ステートマシンに以下の防御策を組み込みます。

1. **LEN の不正値ガード（バッファオーバーラン防御）**
   受信した `LEN` が許容上限（64バイト）を超えていた場合、直ちに受信を中断（`abort`）してバスを沈黙させます。
   ```c
   #define MAX_PAYLOAD_SIZE (64)

   void on_frame_len_received(uint8_t len) {
       if (len > MAX_PAYLOAD_SIZE) {
           // ノイズまたは不正フレームと判定し即時破棄
           // バトンを落とし、パッシブ・フェイルセーフへ移行
           abort_reception_and_silence();
       } else {
           expected_remaining_bytes = len + 2; // PAYLOAD(len) + CRC16(2)
       }
   }
   ```
2. **CRC-16-CCITT による誤り検出**
   多項式 $x^{16} + x^{12} + x^5 + 1$（初期値: `0xFFFF`）を採用。長尺データやインバータ等の産業ノイズ下でも誤検出見逃し率を極限まで低減します。

---

## 5. 決定論的バトンリレーとサイクルタイム (Deterministic Relay & Timing)

### 5.1 平常時リレーシーケンス

各ノードは **「自機のID（`MY_ID`）」** と **「次にバトンを渡すノードのID（`NEXT_ID`）」** の2つのパラメータのみを保持します。

```text
[Node 1] ───(Target: 2)───> [Node 2] ───(Target: 3)───> [Node 3] ───(Target: 1)───> [Node 1] ...
   │                           │                           │                           │
  (1) 発話                    (2) 発話                    (3) 発話                    (1) 次周発話
```

1. 全ノードは常時バスを受信待機。
2. 正常なフレームを受信し CRC が一致した場合、生存監視用ハードウェアタイマーをリセット。
3. `TARGET_ID == MY_ID` の場合、即座に自身のデータを付与したフレーム（`TARGET_ID = NEXT_ID`）を送信。

---

### 5.2 運用ポリシー：固定長運用 vs 可変長運用

システム設計者は、用途に応じて以下の2つのポリシーから選択します。

* **ポリシーA: 完全固定長運用（厳密なハードリアルタイム）**
  * 全ノードの `LEN` を設計時に固定（例: 全ノード 16バイト）。
  * 実行時のフレーム長変動がゼロとなり、通信ジッターはマイコン内部RCの微小な短期周波数ジッター（$\mu\text{s}$ 未満）のみに収束します。
* **ポリシーB: 最大長拘束型・可変長運用（帯域最適化）**
  * ノードごとに必要なデータ量に応じて `LEN` を動的に変更。
  * 最悪実行時間（WCET）は各ノードの「許容最大データ長 $L_{\max}(i)$」から静的に算出し、タイムアウト値を設計します。

---

### 5.3 最悪実行時間（WCET）およびサイクルタイム算定モデル

ノード総数を $N$、ボーレートを $B$ [bps]、1バイトあたりの伝送ビット数を $b_{\text{byte}} = 10$（Start + Data 8 + Stop）、物理同期ヘッダ時間を $T_{\text{header}}$、送受信切替ギャップを $T_{\text{gap}}$ と置いたとき、ノード $i$ のフレーム送信時間 $T_{\text{node}}(i)$ は以下で求まります。

$$T_{\text{node}}(i) = T_{\text{header}} + \frac{(3 + L(i) + 2) \times b_{\text{byte}}}{B} + T_{\text{gap}}$$

ここで $3 = \text{LEN}(1\text{B}) + \text{TARGET\_ID}(1\text{B}) + \text{SENDER\_ID}(1\text{B})$、$2 = \text{CRC16}(2\text{B})$ です。

システム全体の平常時サイクルタイム $T_{\text{cycle}}$ は全ノードの送信時間の総和となります。

$$T_{\text{cycle}} = \sum_{i=1}^{N} T_{\text{node}}(i)$$

再送や衝突待機（バックオフ）がプロトコルに存在しないため、**$T_{\text{cycle}}$ は設計段階で完全に一意に定まります。**

---

## 6. メディエーション（調停）と例外・保守シーケンス (Mediation & Fault Tolerance)

### 6.1 パッシブ・フェイルセーフ（断線・ノード故障）

ノードの故障や通信ケーブルの断線により、特定のノードがバトンを受け取れず沈黙した場合：

```text
[Node 1] ──(Target: 2)──> [Node 2 (沈黙 / 故障)]
                               │ (バトン落下 = バス無音)
                               ▼
                    【全ノード タイマー満了】
                               │
                               ▼
               【全ノード 同期して安全停止 (STO)】
```

* **自律心中:** 各ノードは勝手にスキップや再送を試みることなく、監視タイマー満了により出力を即座に遮断（STO）し、沈黙を維持します。

---

### 6.2 意図的衝突（Forced Collision）による強制停止

稼働中のリレーを安全に停止させたい場合（Mediatorによる介入、保守作業時など）、Mediator はバスへ規定長以上のドミナント信号（Breakパルス）を強制送出します。

```text
[通常リレー中]
Node 1 ──> Node 2 ──> Node 3 ...
                       ▲
[Mediator 介入]        │ 強制 Break パルス注入（物理衝突）
                       ▼
            [全ノード CRC破損 / フレーミング異常検知]
                       │
                       ▼
            [全ノード 送信中断 ＆ 安全待機へ移行]
```

* 送信中のノードはローカルループバックの不一致により直ちに送信をアボート。
* 受信中のノードはCRCエラーまたはフレーミング異常によりパケットを破棄し、安全停止タイマー待機状態へ遷移します。

---

### 6.3 再点火フレーム（Re-Ignition Frame）とリレー再開

バスが静寂状態（無音時間 $> T_{\text{timeout}}$）にあることを確認した Mediator は、正規の物理同期ヘッダを持つ再点火フレームを送出します。

```text
[ Break Field ] ──> [ Sync (0x55) ] ──> [ LEN ] ──> [ TARGET_ID = X ] ──> [ SENDER_ID = MEDIATOR ] ...
```

* 指名された Node $X$ は、直前のノードからのバトンか Mediator からの再点火かを区別せず、`TARGET_ID == MY_ID` を検知して通常リレーを再開します。
* **末端ノードのファームウェアには、再点火のための特別な例外処理コードは一切不要です。**

---

### 6.4 コールドスタート（初期起動）シーケンス

システム電源投入時における初回点火の作法：

1. **Mediator が存在する場合:**
   電源投入後、全ノードの起動完了を待機（$T_{\text{boot\_margin}}$）した後、Mediator が Node 1 宛ての再点火フレームを打ち込んでリレーを開始。
2. **完全マスターレス構成の場合:**
   Node 1 のみに「起動時初回点火フラグ（`INITIATOR`）」を設定し、パワーオンリセット後、バスが無音であることを確認した上で最初のフレームを送出。

---

### 6.5 ハードウェア故障耐性（Jabber対策）
* 1つのノードがマイコン暴走等により TX ピンを Low（ドミナント）固定にし続けた場合、バス全体が専有されます。
* これを防ぐため、ハードウェア側に TX ドミナント・タイムアウト保護付きトランシーバを採用するか、マイコンのハードウェア・ウォッチドッグタイマー（WDT）による強制リセット機構を併用します。

---

## 7. 再送プラグイン (Retry Plugin) アーキテクチャ

### 7.1 プロトコル・コアからの分離

DROP-Bus Core は一切の再送ロジックを持ちません。再送を許容するシステムを構築する場合、**Mediator ロールを持つエンティティにのみ「再送プラグイン」を組み込みます。**

```text
+-------------------------------------------------------------------+
|                        DROP-Bus Core                              |
|   - LINAUTO 物理同期 (Break + 0x55)                               |
|   - 固定長 / 最大長拘束型可変長リレー                              |
|   - ハードウェアタイマー監視 ＆ パッシブ・フェイルセーフ (STO)     |
|   - ゼロ再送・即時ドロップ                                        |
+-------------------------------------------------------------------+
                                  ▲
                       フック呼び出し (Hook API)
                                  │
+---------------------------------┴---------------------------------+
|                    Retry Plugin (任意モジュール)                  |
|   - 異常検知 (CRC破損 / 無音タイムアウト)                         |
|   - 適用対象フィルタリング (特定ノードID / 全ノード一律)          |
|   - 実行制御 (最大リトライ回数 / 許容時間枠 / クールダウン)       |
|   - 調停シグナル送出 (Break衝突による停止 / 再点火フレーム生成)   |
+-------------------------------------------------------------------+
```

---

### 7.2 データ構造と API 仕様 (C言語)

```c
#include <stdint.h>
#include <stdbool.h>

#define DROP_ID_ALL   (0xFF)  ///< 全ノード一括指定用ワイルドカードID

/**
 * @brief 再送判定の契機となるエラー要因
 */
typedef enum {
    DROP_TRIGGER_NONE       = 0x00,
    DROP_TRIGGER_CRC_ERROR  = 0x01,  ///< CRC不一致によるパケット破損
    DROP_TRIGGER_TIMEOUT    = 0x02,  ///< 規定時間内に次のフレームが来ない（無音途絶）
    DROP_TRIGGER_BOTH       = 0x03   ///< CRC破損・無音途絶の双方を対象とする
} DropRetryTrigger_t;

/**
 * @brief プラグインがコアへ指示する調停アクション
 */
typedef enum {
    DROP_ACTION_DROP_AND_STOP = 0,   ///< 再送を行わず安全停止状態へ誘導（受動的STO）
    DROP_ACTION_RETRY                ///< 再点火フレームを送出し、バトンを再始動させる
} DropAction_t;

/**
 * @brief 再送ポリシー定義構造体
 */
typedef struct {
    uint8_t            target_id;    ///< 対象ノードID（個別ID または DROP_ID_ALL）
    DropRetryTrigger_t trigger;      ///< 監視対象トリガー
    uint8_t            max_retries;  ///< 最大リトライ許容回数（0 で即時STO）
    uint16_t           timeout_us;   ///< 無音監視タイムアウト時間（マイクロ秒）
    uint16_t           cooldown_us;  ///< 再点火フレーム送出前のバス安定化待機時間
} DropRetryPolicy_t;

/**
 * @brief 再送ポリシー評価ハンドラ
 */
DropAction_t drop_retry_evaluate(
    const DropRetryPolicy_t *policies,
    uint8_t policy_count,
    uint8_t failed_target_id,
    DropRetryTrigger_t trigger,
    uint8_t *retry_counter
);
```

---

### 7.3 適用ユースケースと最悪遅延の算定

#### ユースケース 1: 過酷環境区間の局所リトライ
可動ケーブルベアやスリップリングを経由する特定ノード（例: Node `0x04`）のみ 1 回のリトライを許可し、他は即時停止させる構成。

```c
const DropRetryPolicy_t local_resilient_policy[] = {
    // Node 0x04 宛て: CRCエラー時のみ1回だけ再送を試行
    {
        .target_id   = 0x04,
        .trigger     = DROP_TRIGGER_CRC_ERROR,
        .max_retries = 1,
        .timeout_us  = 500,
        .cooldown_us = 50
    },
    // 全体デフォルト: いかなるエラーでも即時安全遮断（リトライゼロ）
    {
        .target_id   = DROP_ID_ALL,
        .trigger     = DROP_TRIGGER_BOTH,
        .max_retries = 0,
        .timeout_us  = 800,
        .cooldown_us = 0
    }
};
```
* **最悪遅延の数学的保証:** 通常サイクルタイムに対し、最悪遅延枠が $+550\,\mu\text{s}$ に収まることが数式上で証明可能です。

#### ユースケース 2: 同期多軸ロボット（完全決定論・即時心中）
1フレームの欠落も許されない厳密同期環境。

```c
const DropRetryPolicy_t strict_deterministic_policy[] = {
    {
        .target_id   = DROP_ID_ALL,
        .trigger     = DROP_TRIGGER_BOTH,
        .max_retries = 0,            // リトライゼロ
        .timeout_us  = 300,
        .cooldown_us = 0
    }
};
```

---

## 8. 比較優位性マトリクスと規格適合性 (Comparative Analysis & Compliance)

### 8.1 従来規格との比較

| 評価軸 | LIN | CAN / CAN-FD | Modbus-RTU | 一般的なトークンリング | **DROP-Bus** |
| :--- | :---: | :---: | :---: | :---: | :---: |
| **マスター依存性** | 常時必須 (SPOF) | 不要 (マルチマスター) | 常時必須 (SPOF) | 不要 | **平常時不要 (オプション化)** |
| **クロック要件** | 水晶レス (LINAUTO) | 高精度水晶必須 | 水晶推奨 | 高精度水晶必須 | **完全水晶レス (LINAUTO)** |
| **通信ジッター** | 小 (ポーリング固定) | 大 (優先度・再送変動) | 中 (応答ばらつき) | 小 | **極小 (数式で拘束可能)** |
| **通信効率** | 低 (ヘッダ過多) | 中 (調停オーバーヘッド) | 低 (呼出/応答2往復) | 高 | **最高 (純粋リレー)** |
| **最大データ長** | 8 Byte | 8 / 64 Byte | 252 Byte | 自由 | **最大 64 Byte** |
| **異常時動作** | マスター再送依存 | 自動再送で遅延拡大 | タイムアウト待ち | トポロジー再構成遅延 | **受動的即時停止 (心中STO)** |
| **末端ノード単価** | 低 | 中〜高 (CANコントローラ) | 低〜中 | 高 (専用IC/PHY) | **極低 (超安価MCU単一SKU)** |

---

### 8.2 機能安全規格（IEC 61508 / ISO 13849）適合への説明性

1. **ブラックボックスの排除**
   プロトコルスタックが裏で勝手な再送やバックオフを行わないため、安全度水準（SIL / PL）の評価に必要な安全機能の最悪応答時間（Worst-Case Response Time）が明瞭に算出できます。
2. **単一故障時の安全側動作（Fail-Safe State）**
   断線、ノード電源喪失、CRC破損などのあらゆる単一故障において、プロトコルは「バス無音 $\rightarrow$ 全ノードタイマー満了 $\rightarrow$ ハードウェアSTO」へと一意に収束します。

---

## 9. 実装ガイドラインとロードマップ (Implementation Guidelines)

### 9.1 ADX Core-D（ATtiny1616）リファレンス実装
ADX Core-D における推奨ペリフェラル割り当て：

* **USART0 (LINAUTO モード):** `PA1` (TXD), `PA2` (RXD)
* **RS-485 方向制御 (DE/RE):** `PA4` / `PA7`（送受信切り替え）
* **生存監視タイマー:** 16-bit タイマー `TCB0`（ワンショットまたは周期モード）
* **安全遮断出力 (STO):** イベントシステム（EVSYS）経由でタイマー満了時にダイレクトにPWM/GPIO出力をLowへ固定遮断

---

### 10. まとめ

DROP-Bus は、無駄な通信オーバーヘッドやブラックボックスな自動再送を徹底的に削ぎ落とし、**「沈黙＝安全」という産業用制御の根本原則**の上に構築された革新的な自律分散通信プロトコルです。

低コストな単一SKUハードウェアでありながら、最高峰のリアルタイム性と機能安全説明性を両立し、次世代の分散制御ハードウェアに確固たる通信基盤を提供します。
